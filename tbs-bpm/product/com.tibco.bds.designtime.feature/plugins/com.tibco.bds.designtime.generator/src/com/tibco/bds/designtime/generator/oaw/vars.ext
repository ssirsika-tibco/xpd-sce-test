// There's no apparent need to type vars that are already strings, but docs say it's good practice to always
// type and they use a String var in the example. It also reduces length at point of use.

String projectVersion() : GLOBALVAR projectVersion;


