#The basedir you want to use
basedir = .

#The outlet to use to store code artefacts
outlet.src.dir =${basedir}/src/generated/java/default

# The outlet to use to store non-code artefacts (configurations, sql-files and so on)
outlet.res.dir =${basedir}/src/generated/resources

#The outlet to use to store code artefacts. At this the generator will checking 
#whether the artifact to generate already exists. If yes the generation of this 
#artifact will be skipped.
outlet.src.once.dir = ${basedir}/src/generated/java/override

#The outlet to use to store non-code artefacts. At this the generator will checking 
#whether the artifact to generate already exists. If yes the generation of this 
#artifact will be skipped.
outlet.res.once.dir = ${basedir}/src/main/resources

#The modelfile
# model.file =src/main/resources/MyDomain.uml
model.file = D:/temp/business_studio-A4-1hour/eclipse/runtime-New_configuration/test%20xsd/models/MyDomain.uml

#The persistence profile
# profile.persistence.file = src/main/resources/Persistence.profile.uml
profile.persistence.file = D:/temp/business_studio-A4-1hour/eclipse/runtime-New_configuration/test%20xsd/models/Persistence.profile.uml

#The properties file
hibernate.properties.file = 'hibernateCartridge.properties'